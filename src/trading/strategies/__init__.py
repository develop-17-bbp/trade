"""Strategy modules for ACT.

Each module is a self-contained strategy or pattern detector that
produces a signal dict. Strategies are exposed to the LLM brain via
tools in `src/ai/unified_brain_tools.py`.
"""
