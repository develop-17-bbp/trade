# Core
numpy==2.4.2
pandas==2.3.3

# Technical Analysis
ta==0.11.0

# Data
requests==2.32.5
ccxt==4.5.40

# AI / NLP
transformers==5.2.0
sentence-transformers==5.2.3
torch==2.10.0
optuna==4.7.0
scipy==1.17.1

# Config
python-dotenv==1.2.2
pyyaml==6.0.3

# ML Models
lightgbm==4.6.0

# Layer 6+: Agentic & Fine-Tuning
pydantic==2.12.5

# Phase 0: decision_id plumbing — stdlib-only ULID in src/orchestration/_ulid.py
# (dropped python-ulid dep because of a name collision with another PyPI `ulid`
# package that ships as a single module without a ULID class)

# Phase 4.5a: Ridge regression for CreditAssigner. Already transitively installed
# via lightgbm; pinned explicitly so Phase 4.5a has a hard guarantee.
scikit-learn==1.8.0

# Phase 1: observability stack. All three are SOFT deps in the orchestration
# package — the bot still boots if they're missing, it just won't emit metrics
# or spans. OTel 1.41+ is required for Python 3.14 (earlier versions pin
# protobuf <5 which trips Python 3.14's stricter metaclass rules).
prometheus-client==0.21.0
opentelemetry-api==1.41.0
opentelemetry-sdk==1.41.0
opentelemetry-exporter-otlp-proto-grpc==1.41.0

# Phase 2: resilience stack. Also soft deps — bot boots fine without them.
# redis    — streams + hot state store (requires Redis running in infra/)
# pybreaker — named circuit breakers replacing the ad-hoc one in base_fetcher
# tenacity  — retry-with-backoff wrappers for flaky external feeds
redis==5.1.0
pybreaker==1.2.0
tenacity==9.0.0

# Dashboard
flask==3.1.3
streamlit==1.55.0
fastapi==0.135.1
uvicorn==0.41.0
plotly==6.6.0

# Testing
pytest==9.0.2

# Institutional Infrastructure v6.5
msgpack==1.1.2
clickhouse-connect==0.13.0

# LLM Providers
google-genai>=1.0.0

# Security
cryptography==46.0.5

# Dual-asset (Phase B/C/D — stocks ACT on the 4060)
# alpaca-py is the official Anthropic-style SDK; we use alpaca-trade-api for the
# AlpacaClient in src/data/fetcher.py + raw REST for AlpacaFetcher.
# pandas_market_calendars provides the live NYSE schedule with holidays + early closes;
# src/utils/market_hours.py falls back to a hardcoded 2026 list if missing.
# pyarrow drives compact parquet deltas for warm_store_sync; falls back to JSON.
alpaca-py>=0.30.0
pandas_market_calendars>=4.4.0
pyarrow>=18.0.0

# Optional install-manually-on-the-GPU-box (real QLoRA fine-tune path):
#   peft, bitsandbytes, accelerate, trl  — paired with Unsloth on the
#   training machine. Not in main reqs because they pull torch-build
#   constraints that would break the inference-only boxes.
# Optional Polymarket live (commented because most operators stay shadow-mode):
#   py-clob-client>=0.1.0
# Optional Polygon historical (survivorship-bias-aware backtests, training-only):
#   polygon-api-client>=2.0.0
#
# Removed 2026-04-29 (no live importer, no near-term plan):
#   pytorch-forecasting, llama-cpp-python, PyWavelets, chromadb, kafka-python-ng
